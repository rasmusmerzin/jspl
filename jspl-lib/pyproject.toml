[project]
name = "jspl-lib"
version = "0.1.0"
description = "JsPL common library"
authors = [
    {name = "Emil Rasmus Merzin",email = "rasmusmerzin@gmail.com"}
]
license = "MIT"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
]

[dependency-groups]
dev = [
    "pytest (>=9.0.3,<10.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
